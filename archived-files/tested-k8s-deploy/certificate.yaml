apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: myapp-tls-prod
  namespace: three-tier
spec:
  secretName: myapp-tls-prod
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: mysite06.duckdns.org
  dnsNames:
  - mysite06.duckdns.org
